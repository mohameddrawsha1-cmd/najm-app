{
  "name": "najm/it-service-system",
  "description": "Arabic RTL IT service management system built with PHP and MySQL.",
  "type": "project",
  "require": {
    "php": "^8.2",
    "ext-pdo": "*",
    "ext-mbstring": "*",
    "ext-json": "*"
  },
  "autoload": {
    "psr-4": {
      "App\\": "app/"
    },
    "files": ["app/helpers.php"]
  },
  "scripts": {
    "serve": "php -S 127.0.0.1:8000 -t public",
    "lint": "find app public routes scripts tests -name '*.php' -print0 | xargs -0 -n1 php -l",
    "test": "php tests/smoke.php"
  }
}
